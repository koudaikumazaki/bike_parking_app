
<div class="text-center">
  <h2 class="text-2xl sm:text-3xl my-10">パスワードの再設定</h2>

  <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, role: "form" }) do |f| %>
  <%
=begin%>
 <%= form_for(resource, :as => resource_name, :url => new_user_password_path, :html => { :method => :post, role: "form" }) do |f| %>  
<%
=end%>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <%= f.label :email, "メールアドレス" %><br />
      <%= f.email_field :email, class: 'border', placeholder: "メールアドレスを入力してください", autofocus: true, autocomplete: "email" %>
    </div>

    <div class="actions my-5">
      <%= f.submit "パスワード再設定用のメールを送る", class: 'p-2 button' %>
    </div>
  <% end %>
</div>


<%= render "devise/shared/links" %>
